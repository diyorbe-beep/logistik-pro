# JWT Configuration - REQUIRED FOR PRODUCTION
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters-long-for-production-security
JWT_SECRET_PREVIOUS=previous-jwt-secret-for-rotation-support

# Server Configuration
PORT=10000
NODE_ENV=production

# Security Settings
BCRYPT_ROUNDS=12

# Logging
LOG_LEVEL=info

# CORS Settings - Add your frontend domains
ALLOWED_ORIGINS=https://logistik-dusky.vercel.app,https://logistik-pro.onrender.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Render.com specific settings
# Make sure to set these environment variables in your Render.com dashboard:
# 1. JWT_SECRET (required)
# 2. NODE_ENV=production
# 3. PORT=10000 (Render.com will override this automatically)